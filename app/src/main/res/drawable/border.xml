<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- lobby background color -->
    <solid
        android:color="@color/lobby_background_color" >
    </solid>

    <!-- lobby border color and width -->
    <stroke
        android:width="2dp"
        android:color="@color/lobby_border_color" >
    </stroke>

    <!-- The radius makes the corners rounded -->
    <corners
        android:radius="5dp"   >
    </corners>

</shape>